import { ILabel } from "./Label";

const base: ILabel = {
  id: "label-id",
  isRequired: true,
  text: "Label",
  cssClasses: "label css-classes",
  children: "Label children",
};

export const LabelMockProps = {
  base,
};
