import { ITemplateComponent } from "./TemplateComponent";

const Base: ITemplateComponent = {
  children: "Base",
};

const TemplateComponentMockProps = {
  Base: Base,
};

export default TemplateComponentMockProps;
